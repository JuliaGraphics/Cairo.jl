matrix:
    include:
       - language: julia
         julia: 0.5
         os: linux
         dist: trusty
         sudo: false         
       - language: julia
         julia: 0.6 
         os: linux
         dist: trusty
         sudo: false         
       - language: julia
         julia: nightly 
         os: linux
         dist: trusty
         sudo: false         
notifications:
    email: false
after_success:
    - julia -e 'cd(Pkg.dir("Cairo")); Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder())'
